import 'package:flutter/material.dart';
import 'login_page.dart'; // Impor halaman LoginPage

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    home: LoginPage(), // Gunakan LoginPage sebagai halaman utama
  ));
}
